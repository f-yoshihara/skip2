<h1>ストック一覧</h1>
<% if @stocks.empty? %>
  <h2>まだストックはありません。</h2>
<% else %>
  <% @stocks.each do |s| %>
    <h2><%= s.recruitment.title %></h2>
    <p><%= s.recruitment.body %></p>
  <% end %>
<% end %>