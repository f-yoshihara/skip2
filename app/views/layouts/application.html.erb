<!DOCTYPE html>
<html>
  <head>
    <title>Trigger</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1">
  </head>

  <body>
    <nav class="navbar navbar-expand-lg fixed-top">
      <%= link_to root_path do%>
        <i class="navbar-brand">Trigger</i>
      <% end %>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <%= link_to login_logout_path do%>
            <i class='nav-link'>ログアウト</i>
            <% end %>
          </li>

          <li class="nav-item">
            <%= link_to '採用担当者新規登録', staff_signup_index_path, class: :'nav-link'%>
          </li>

          <% if session[:staff] %>
            <li class="nav-item">
              <%= link_to '採用者用管理ページ', recruitment_list_index_path, class: :'nav-link'%>
            </li>

          <% else %>
            <li class="nav-item">
              <%= link_to '企業担当者ログイン', staff_login_index_path, class: :'nav-link'%>
            </li>
          <% end %>
        
          <% if current_user %>
            <li class="nav-item">
              <%= link_to 'マイページ', user_path(current_user), class: :'nav-link' %>
            </li>

            <li class="nav-item">
              <%= link_to 'お気に入り', stocks_path(id: current_user), class: :'nav-link' %>
            </li>

            <li class="nav-item">
              <%= link_to '応募一覧', entries_path(id: current_user), class: :'nav-link' %>
            </li>

          <% else %>
            <li class="nav-item active">
              <%= link_to 'ログイン', login_index_path, class: :'nav-link'%>
            </li>
          <% end %>
        </ul>

        <% if current_user %>
          <p>ようこそ<%= current_user.name %>さん</p>
        <% end %>
        <%# <form class="form-inline my-2 my-lg-0">
          <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form> %>
      </div>
    </nav>
  
    <%= yield %>
    <%# ↓deviseの設定。%>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%# firebase %>
    <script src="https://www.gstatic.com/firebasejs/5.5.3/firebase.js"></script>
  </body>
</html>
